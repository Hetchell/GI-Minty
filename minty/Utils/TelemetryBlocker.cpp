#include "../includes.h"
#include <fstream>
#include <filesystem>
#include <optional>
#include "TelemetryBlocker.h"

void TelemetryBlocker::BlockTelemetry() {
    // TODO: Create code to remove telemetry blocking from /etc/hosts for users who used the old method.
}

